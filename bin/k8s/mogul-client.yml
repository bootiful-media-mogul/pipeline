apiVersion: apps/v1
kind: Deployment
metadata:
  name: mogul-client-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mogul-client
  template:
    metadata:
      labels:
        app: mogul-client
    spec:
      containers:
        - name: mogul-client-container
          image: us-docker.pkg.dev/joshlong/mogul-artifact-registry/authorization-service:latest
          imagePullPolicy: Always
          envFrom:
            - secretRef:
                name: mogul-secrets
          env:
            - name: SPRING_APPLICATION_NAME
              value: "mogul-client"
            - name: SERVER_PORT
              value: "8080"
            - name: LANG
              value: "en_US.UTF-8"


---
apiVersion: networking.gke.io/v1
kind: ManagedCertificate
metadata:
  name: mogul-client-certificate
spec:
  domains:
    - "studio.media-mogul.io"

---
apiVersion: v1
kind: Service
metadata:
  name: mogul-client-service
spec:
  type: NodePort
  selector:
    app: mogul-client
  ports:
    - port: 8080
      targetPort: 8080

---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: mogul-client-ingress
  annotations:
    ingress.kubernetes.io/force-ssl-redirect: "true"
    kubernetes.io/ingress.global-static-ip-name: "mogul-mogul-client-ip"
    networking.gke.io/managed-certificates: "mogul-client-certificate"
spec:
  rules:
    - host: studio.media-mogul.io
      http:
        paths:
          - pathType: ImplementationSpecific
            path: "/*"
            backend:
              service:
                name: mogul-client-service
                port:
                  number: 8080
