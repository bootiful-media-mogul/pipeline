apiVersion: v1
kind: Service
metadata:
  annotations:
    cloud.google.com/neg: '{"ingress": true}'
  labels:
    app: mogul-service
  name: mogul-service
spec:
  ports:
    - port: 80
      targetPort: 8080
      name: http
  selector:
    app: mogul-service
  type: LoadBalancer